kolla_base_distro: "{{ kolla_base_distro }}"
kolla_internal_vip_address: "{{ kolla_internal_vip_address }}"
kolla_container_engine: "{{ kolla_container_engine }}"
network_interface: "{{ network_interface }}"
api_interface: "{{ network_interface }}"
tunnel_interface: "{{ tunnel_interface }}"
network_address_family: "{{ network_address_family }}"
neutron_external_interface: "{{ neutron_external_interface }}"
neutron_plugin_agent: "{{ neutron_plugin_agent }}"
enable_neutron_packet_logging: "no"
kolla_enable_tls_internal: "{{ kolla_enable_tls_internal }}"
kolla_enable_tls_backend: "{{ kolla_enable_tls_backend }}"
openstack_region_name: "{{ openstack_region_name }}"
enable_haproxy: "{{ enable_haproxy }}"
enable_cinder: "{{ enable_cinder }}"
rabbitmq_enable_tls: "{{ rabbitmq_enable_tls }}"
glance_backend_file: "{{ glance_backend_file }}"
nova_compute_virt_type: "kvm"
nova_console: "novnc"
